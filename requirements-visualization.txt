# Additional requirements for visualization examples
# Install these for examples that use matplotlib for plotting and visualization

# Core plotting library
matplotlib>=3.3.0

# For better GUI support on different platforms:

# macOS: Use built-in backend (usually works out of the box)
# No additional packages needed for macOS

# Linux: Qt backend (recommended)
# PyQt5>=5.15.0

# Windows: Qt backend (recommended) 
# PyQt5>=5.15.0

# Alternative: Tkinter support (if needed)
# Linux: sudo apt-get install python3-tk  
# macOS: brew install python-tk
# Windows: Usually included with Python

# Image processing (for examples that need it)
opencv-python>=4.5.0
Pillow>=8.0.0